# https://houndci.com/configuration

ruby:
  enabled: true
  # config_file: config/.rubocop.yml

coffee_script:
  enabled: true
  # config_file: config/.coffeelint.json

java_script:
  enabled: true
  # config_file: config/.jshint.json
  # ignore_file: config/.javascript_ignore # ingore path/file per line

scss:
  enabled: true
  # config_file: config/scss.yml
